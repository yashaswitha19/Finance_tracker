<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .main-content {
            margin-left: 280px;
            /* Add padding to the container */
            padding: 1.5rem;
        }

        /* Responsive handling for sidebar */
        @media (max-width: 992px) {
            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>

<body>
    <div class="d-flex">
        <%- include('partials/sidebar', {Active: 'home',user: user }) %>
           
            <div class="main-content w-100">
                <div class="container-fluid p-0">

                    <div class="row border-bottom pb-3 mb-4">
                        <div class="col-12">
                            <h2>Hello <%=user.name%>!</h2>
                            <p class="text-muted">Here is your financial progress for this month.</p>
                        </div>
                    </div>

                    <div class="row g-4 mb-4">
                        <div class="col-xl-3 col-md-6">
                            <div class="card border-success shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <p class="text-muted mb-1">Total Income</p>
                                            <h3 class="mb-0 text-success fw-bold">₹<%=
                                                    totalincome.toLocaleString('en-IN') %>
                                            </h3>
                                        </div>
                                        <div class="bg-success bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-arrow-up-circle text-success" style="font-size: 2rem;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card border-danger shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <p class="text-muted mb-1">Total Expense</p>
                                            <h3 class="mb-0 text-danger fw-bold">₹<%=
                                                    totalexpense.toLocaleString('en-IN') %>
                                            </h3>
                                        </div>
                                        <div class="bg-danger bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-arrow-down-circle text-danger" style="font-size: 2rem;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card border-primary shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <p class="text-muted mb-1">All-Time Balance</p>
                                            <h3 class="mb-0 text-primary fw-bold">₹<%=
                                                    allTimeBalance.toLocaleString('en-IN') %>
                                            </h3>
                                        </div>
                                        <div class="bg-primary bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-wallet2 text-primary" style="font-size: 2rem;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card border-warning shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <p class="text-muted mb-1">Budget Remaining</p>
                                            <h3 class="mb-0 text-warning fw-bold">₹<%=
                                                    budgetRemaining.toLocaleString('en-IN') %>
                                            </h3>
                                        </div>
                                        <div class="bg-warning bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-piggy-bank text-warning" style="font-size: 2rem;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row g-4 mb-4">
    <div class="col-lg-12  col-md-10 ">
        <div class="card shadow-sm" style="min-height: 350px;">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">Spending by Category (This Month)</h5>
                <% if (pieChartData && pieChartData.labels.length > 0) { %>
                    <div class="position-relative flex-grow-1">
                        <canvas id="categoryPieChart"></canvas>
                    </div>
                <% } else { %>
                    <div class="d-flex flex-column align-items-center justify-content-center text-muted flex-grow-1">
                        <i class="bi bi-pie-chart" style="font-size: 3rem; opacity: 0.5;"></i>
                        <p>No spending data for this month.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div> <div class="row g-4 mb-4">
    <div class="col-lg-12  col-md-10 ">
        <div class="card shadow-sm" style="min-height: 350px;">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">Income vs Expense (Last 6 Months)</h5>
                <% if (barChartData && barChartData.labels.length > 0) { %>
                    <div class="position-relative flex-grow-1">
                        <canvas id="incomeExpenseBarChart"></canvas>
                    </div>
                <% } else { %>
                    <div class="d-flex flex-column align-items-center justify-content-center text-muted flex-grow-1">
                        <i class="bi bi-bar-chart" style="font-size: 3rem; opacity: 0.5;"></i>
                        <p>No data available.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div> <div class="row g-4 mb-4">
    <div class="col-lg-12  col-md-10 ">
        <div class="card shadow-sm" style="min-height: 350px;">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">Monthly Net (Last 6 Months)</h5>
                <% if (barChartData && barChartData.labels.length > 0) { %>
                    <div class="position-relative flex-grow-1">
                        <canvas id="balanceLineChart"></canvas>
                    </div>
                <% } else { %>
                    <div class="d-flex flex-column align-items-center justify-content-center text-muted flex-grow-1">
                        <i class="bi bi-graph-up" style="font-size: 3rem; opacity: 0.5;"></i>
                        <p>No data available.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>
                    <div class="row g-4 mb-4">
                        <div class="col-12">
                            <div class="card shadow-sm">
                                <div
                                    class="card-header bg-white d-flex justify-content-between align-items-center py-3">
                                    <h6 class="mb-0"><i class="bi bi-clock-history me-2"></i> Recent Transactions</h6>
                                    <a href="/managetr" class="btn btn-sm btn-outline-primary">View All</a>
                                </div>
                                <div class="card-body p-0">
                                    <div class="table-responsive">
                                        <table class="table table-hover mb-0">
                                            <thead class="table-light">
                                                <tr>
                                                    <th>Date</th>
                                                    <th>Type</th>
                                                    <th>Category</th>
                                                    <th>Amount</th>
                                                    <th>Note</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (transactions && transactions.length> 0) { %>
                                                    <% transactions.forEach(function(transaction) { %>
                                                        <tr>
                                                            <td>
                                                                <%= new
                                                                    Date(transaction.date).toLocaleDateString('en-GB')
                                                                    %>
                                                            </td>
                                                            <td>
                                                                <% if (transaction.type==='income' ) { %>
                                                                    <span
                                                                        class="badge bg-success-subtle text-success-emphasis rounded-pill">
                                                                        <i class="bi bi-arrow-up"></i> Income
                                                                    </span>
                                                                    <% } else { %>
                                                                        <span
                                                                            class="badge bg-danger-subtle text-danger-emphasis rounded-pill">
                                                                            <i class="bi bi-arrow-down"></i> Expense
                                                                        </span>
                                                                        <% } %>
                                                            </td>
                                                            <td>
                                                                <%= transaction.category %>
                                                            </td>
                                                            <td>
                                                                <% if (transaction.type==='income' ) { %>
                                                                    <strong class="text-success">+₹<%=
                                                                            parseFloat(transaction.amount).toLocaleString('en-IN')
                                                                            %></strong>
                                                                    <% } else { %>
                                                                        <strong class="text-danger">-₹<%=
                                                                                parseFloat(transaction.amount).toLocaleString('en-IN')
                                                                                %></strong>
                                                                        <% } %>
                                                            </td>
                                                            <td class="text-muted">
                                                                <%= transaction.note || '-' %>
                                                            </td>
                                                        </tr>
                                                        <% }); %>
                                                            <% } else { %>
                                                                <tr>
                                                                    <td colspan="5" class="text-center text-muted p-4">
                                                                        No transactions found
                                                                    </td>
                                                                </tr>
                                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function () {

            const chartOptions = {
                responsive: true,
                maintainAspectRatio: false, // This is key to filling the card
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: {
                            padding: 15,
                            font: { size: 12 }
                        }
                    }
                }
            };

            // === 1. Spending by Category (Pie Chart) ===
            <% if (pieChartData && pieChartData.labels.length > 0) { %>
                const pieCtx = document.getElementById('categoryPieChart');
                if (pieCtx) {
                    new Chart(pieCtx, {
                        type: 'pie',
                        data: {
                            labels: <%- JSON.stringify(pieChartData.labels) %>,
                            datasets: [{
                                data: <%- JSON.stringify(pieChartData.data) %>,
                                backgroundColor: ['#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF']
                            }]
                        },
                        options: chartOptions // Use shared options
                    });
                }
            <% } %>

            // === 2. Income vs Expense (Bar Chart) ===
            <% if (barChartData && barChartData.labels.length > 0) { %>
                const barCtx = document.getElementById('incomeExpenseBarChart');
                if (barCtx) {
                    new Chart(barCtx, {
                        type: 'bar',
                        data: {
                            labels: <%- JSON.stringify(barChartData.labels) %>,
                            datasets: [
                                {
                                    label: 'Income',
                                    data: <%- JSON.stringify(barChartData.income) %>,
                                    backgroundColor: 'rgba(40, 167, 69, 0.7)' // Green
                                },
                                {
                                    label: 'Expense',
                                    data: <%- JSON.stringify(barChartData.expense) %>,
                                    backgroundColor: 'rgba(220, 53, 69, 0.7)' // Red
                                }
                            ]
                        },
                        options: { ...chartOptions, scales: { y: { beginAtZero: true } } }
                    });
                }

                // === 3. Monthly Balance Trend (Line Chart) ===
                const lineCtx = document.getElementById('balanceLineChart');
                if (lineCtx) {
                    new Chart(lineCtx, {
                        type: 'line',
                        data: {
                            labels: <%- JSON.stringify(barChartData.labels) %>,
                            datasets: [{
                                label: 'Monthly Net',
                                data: <%- JSON.stringify(barChartData.balance) %>,
                                borderColor: '#2196F3',
                                backgroundColor: 'rgba(33, 150, 243, 0.1)',
                                fill: true,
                                tension: 0.1
                            }]
                        },
                        options: chartOptions // Use shared options
                    });
                }
            <% } %>
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <%- include('partials/chatbot') %>
</body>

</html>